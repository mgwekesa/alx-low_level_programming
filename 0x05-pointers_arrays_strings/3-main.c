#include "main.h"

/**
 * main - Entry point to testing the program
 * Return: returns 0
 */

int main(void)
{
	char *str;

	str = "I do not fear computers. I fear the lack of them - Isaac Asimov";
	_puts(str);
	return (0);
}
